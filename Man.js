class Man {

constructor(x,y,width,height)
{
    var opyions = {
        restitution : 0.2,
        friction :0,
        density : 0.4
    }
    this.Bodies.rectangle (this.position.x,this.position.y,options);
    this.width = width;
    this.height = height;
    this.addImage;
}
display (){
var angle = this.body.angle;
push();
rectMode(CENTER);
image(this.image,0,0,this.width.thid.height);
pop();

}

}